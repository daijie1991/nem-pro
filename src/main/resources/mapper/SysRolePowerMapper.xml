<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nem.pro.modules.sys.mapper.SysRolePowerMapper">

    <resultMap id="SysRolePowerMap" type="com.nem.pro.modules.sys.domain.SysRolePower">
        <id column="id" property="id"></id>
        <result column="power_id" property="powerId"></result>
        <result column="role_id" property="roleId"></result>
    </resultMap>

    <!-- 根据 RoleId 删除角色数据 -->
    <delete id="deleteByRoleId" parameterType="string">
          delete from sys_role_power where role_id = #{roleId}
    </delete>

    <insert id="batchInsert" parameterType="list">
        insert into sys_role_power
        ( id, power_id, role_id )
        values
        <foreach collection ="list" item="item" separator =",">
            (#{item.id}, #{item.powerId}, #{item.roleId})
        </foreach >
    </insert>
</mapper>